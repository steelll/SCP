-- generated by cds-compiler version 1.35.0
COLUMN TABLE my_Employee (
  ID INTEGER NOT NULL,
  NAME NVARCHAR(5000),
  SALARY INTEGER,
  DEPT_ID INTEGER,
  depts_ID INTEGER,
  PRIMARY KEY(ID)
) WITH ASSOCIATIONS (
  MANY TO ONE JOIN my_Dept AS depts ON (depts.ID = depts_ID)
)